[project]
name = "app"  
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "pytauri == 0.8.*",
    "pydantic == 2.*",
    "anyio == 4.*",
    "SQLAlchemy >= 2, < 3",
    "openai >= 1.30.0",
    "python-dotenv >= 1.0.1",
    "agno >= 2.0.0",
    "anthropic >= 0.40.0",
    "groq >= 0.10.0",
    "duckduckgo-search >= 6.0.0",
    "requests >= 2.31.0",
    "docker>=7.1.0",
    "google-genai>=1.47.0",
    "zynk>=0.1.0",
    "ollama>=0.6.1",
]
